@model List<MilasDGaz.Models.Us>
            @foreach(var item in Model)
            {
                <div class="ms-3">
                    <p class="fs-5 fw-medium mb-2 text-white">İletişim İçin</p>
                    <h3 class="m-0 text-secondary">@item.Phone1</h3>
                </div>
            }
